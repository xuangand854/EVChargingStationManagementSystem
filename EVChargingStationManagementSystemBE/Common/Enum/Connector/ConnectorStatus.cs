namespace Common.Enum.Connector
{
    public enum ConnectorStatus
    {
        OutOfService,
        Available,
        InUse,
        Charging,
        Reserved,
        Faulted,
        Unknown,
        Preparing
    }
}
